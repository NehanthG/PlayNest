apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: playnest
spec:
  selector:
    name: backend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      type: ClusterIP
